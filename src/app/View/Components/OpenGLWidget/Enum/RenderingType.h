/**
 * @file RenderingType.h
 * @brief Header file describing the enum RenderingType
 */

#ifndef RENDERING_TYPE_H
#define RENDERING_TYPE_H

/**
 * @brief Enum class describing the type of rendering
 */
enum class RenderingType : int { DEFAULT, SHADERS };

#endif