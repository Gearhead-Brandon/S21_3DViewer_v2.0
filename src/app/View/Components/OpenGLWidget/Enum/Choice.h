/**
 * @file Choice.h
 * @brief Header file describing the enum of Choice
 */

#ifndef CHOICE_H
#define CHOICE_H

/**
 * @brief Enum class describing the choice
 */
enum class Choice : int { Yes, No };

#endif